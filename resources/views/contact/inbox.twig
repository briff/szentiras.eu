{% extends 'layout' %}

{% block content %}
<div class="container mt-4">
    <div class="row">
        <div class="col">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h2 class="h4 mb-0">
                    <i class="bi bi-inbox"></i> Üzeneteim
                </h2>
                <div>
                    <a href="{{ route('contact.form') }}" class="btn btn-primary">
                        <i class="bi bi-envelope-plus"></i> Új üzenet
                    </a>
                </div>
            </div>

            {% if threads.total == 0 %}
                <div class="card">
                    <div class="card-body text-center py-5">
                        <i class="bi bi-inbox text-muted" style="font-size: 3rem;"></i>
                        <h5 class="mt-3">Nincsenek üzenetei</h5>
                        <p class="text-muted">Még nem kapott választ a szerkesztőktől.</p>
                        <a href="{{ route('contact.form') }}" class="btn btn-outline-primary">Üzenet küldése</a>
                    </div>
                </div>
            {% else %}
                <div class="list-group">
                    {% for thread in threads %}
                        <a href="{{ route('contact.thread', thread.id) }}"
                           class="list-group-item list-group-item-action d-flex justify-content-between align-items-start">
                            <div class="ms-2 me-auto">
                                <div class="fw-bold">
                                    {% if thread.sender %}
                                        <span class="badge bg-secondary me-2">Szerkesztő</span>
                                    {% else %}
                                        <span class="badge bg-light text-dark me-2">Ön</span>
                                    {% endif %}
                                    {{ thread.message|slice(0, 100) }}{% if thread.message|length > 100 %}…{% endif %}
                                </div>
                                <small class="text-muted">
                                    <i class="bi bi-clock"></i> {{ thread.latest_activity|date('Y. m. d. H:i') }}
                                    {% if thread.total_replies > 0 %}
                                        • {{ thread.total_replies }} válasz
                                    {% endif %}
                                </small>
                            </div>
                            <div>
                                {% if thread.has_unread %}
                                    <span class="badge bg-danger rounded-pill">Új</span>
                                {% endif %}
                                {% if thread.total_replies > 0 %}
                                    <span class="badge bg-info rounded-pill">{{ thread.total_replies }} válasz</span>
                                {% endif %}
                            </div>
                        </a>
                    {% endfor %}
                </div>

                {% if threads.lastPage > 1 %}
                    <nav class="mt-4">
                        <ul class="pagination justify-content-center">
                            {% if threads.currentPage > 1 %}
                                <li class="page-item">
                                    <a class="page-link" href="{{ threads.previousPageUrl }}">Előző</a>
                                </li>
                            {% endif %}
                            {% for page in 1..threads.lastPage %}
                                <li class="page-item {{ page == threads.currentPage ? 'active' }}">
                                    <a class="page-link" href="{{ threads.url(page) }}">{{ page }}</a>
                                </li>
                            {% endfor %}
                            {% if threads.currentPage < threads.lastPage %}
                                <li class="page-item">
                                    <a class="page-link" href="{{ threads.nextPageUrl }}">Következő</a>
                                </li>
                            {% endif %}
                        </ul>
                    </nav>
                {% endif %}
            {% endif %}
        </div>
    </div>
</div>
{% endblock %}